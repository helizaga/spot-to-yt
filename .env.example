# Spotify API Credentials (REQUIRED)
# Get these from https://developer.spotify.com/dashboard/applications
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8080

# YouTube Music Configuration (REQUIRED)
# Get these from Google Cloud Console after setting up OAuth2 credentials
# Instructions: https://github.com/sigma67/ytmusicapi/wiki/OAuth
YTM_CLIENT_ID=your_youtube_oauth_client_id_here
YTM_CLIENT_SECRET=your_youtube_oauth_client_secret_here
YTM_OAUTH_PATH=oauth.json

# Performance Tuning (Optional)
# CONCURRENT_SEARCHES=10  # Number of parallel search threads (default: 10)
# CONCURRENT_LIKES=5     # Number of parallel like threads (default: 5) 
# MAX_CONCURRENT_REQUESTS=5  # Max concurrent API requests (default: 5)

# Rate Limiting (Optional)
# YTM_MIN_INTERVAL_MS=200  # Minimum milliseconds between API requests (default: 200)

# Note: The script only likes songs on YouTube Music
# It does NOT create playlists (despite what older docs may say)